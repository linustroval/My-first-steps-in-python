import numpy as np

# Array from a list
a = np.array([1, 2, 3])
print(a)

# Zeros/Ones: Useful for initializing grids or electron density maps
zeros = np.zeros(10)
print(zeros)

# Linspace: Creates a reange of numbers. It is useful for plotting
steps = np.linspace(0, 10, 5)
print(steps)
